**What everything is for**

**_loader files are the programs that load the spreadsheet into the database.

**file name - what the file does.

door_loader - loads the doors spreadsheet into the database

doors - the spreadsheet with data that is in the format to be loaded into the database.

doors - the database

price_loader - loads the prices spreadsheet into the database

prices - the spreadsheet of prices formated to load the prices into the database

size_loader - loads the prices spreadsheet into the database

sizes - the spreadsheet of sizes formated to load the sizes into the database

**NOTE** the database doesn't work well with the following 3 characters: , ' "